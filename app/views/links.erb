<% if current_user %>
  Welcome <%= current_user.username %>!
<% end %>

<% @links.each do |link| %>
<li>
  Title: <%= link.title %>
  Link: <%= link.url %>
  Tags:  <%= link.tags.map(&:name).join(", ") %>
</li>
<% end %>
